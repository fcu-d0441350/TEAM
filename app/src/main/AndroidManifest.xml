<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ds95_.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Pet_produce_select"
            android:label="@string/title_activity_pet_produce_select"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Pet_strayData"
            android:label="@string/title_activity_pet_stray_data"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>